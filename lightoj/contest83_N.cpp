#include <iostream>
#include <cmath>

using namespace std;

int main(){
    long long input, numOfZero, numOfOne, currPos, tempIn, result;
    bool firstPosZero, zeroAfterOne, foundOne;
    int t, I, posOfFirstOne, lastOnePos;

    cin >> t;

    for(I = 1; I <= t; I++){

        // integer initialization
        numOfZero = 0;
        numOfOne = 0;
        currPos = -1;


        // bool init
        firstPosZero = false;
        zeroAfterOne = false;
        foundOne = false;


        // input
        cin >> input;
        tempIn = input;

        // input checking
        while(input != 0)



    }

    return 0;
}
